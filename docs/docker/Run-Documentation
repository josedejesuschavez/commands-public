#!/bin/bash

echo "docker"
echo ""
echo "Description:"
echo "Run"
echo ""
echo "-it          -> "
echo "--rm         -> Automatically remove the container when it exits"
echo "-p           -> Publish a container's port(s) to the host"
echo "--restart    -> always, no, unless-stopped, on-failure"
echo "--memory     -> --memory=500m"
echo "--cpus       -> --cpus=1.5"
echo "--cpu-shares -> relative weight"
echo "-v           -> Bind mound a volume"
echo "                -v volumetest:/data"
echo "                -v $PWD:/data"
echo "Final        -> Name image"
